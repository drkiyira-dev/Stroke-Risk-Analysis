# 修改清单

> 生成时间：2026-07-17  
> 目标：修复代码中的事实性错误、一致性问题，并增强分析的完整性

---

## 一、明确的事实性错误

### ✅ 1. task2.py 函数文档字符串步骤编号错位
- **文件**: `task2.py`
- **问题**: 
  - `fill_bmi` 写的是 `①`（模块头注释显示应为 `②`）
  - `map_category_values` 写的是 `③`（应为 `④`）
  - `save_csv` 写的是 `④`（应为 `⑤`）
- **修改**: 修正为与模块头注释一致的编号

### ✅ 2. add_bmi_group 分组标签不精确
- **文件**: `task3.py`
- **问题**: BMI=25.0 的 27 条记录进入超重组，但标签写的是 `'正常(18.5~25)'`，暗示 25 也在正常范围
- **修改**: 标签改为 `'正常(18.5~<25)'` / `'超重(25~<30)'` 以精确表达区间开闭

---

## 二、代码质量和一致性问题

### ✅ 3. map_category_values 原地修改，与 pipeline 其他函数不一致
- **文件**: `task2.py`
- **问题**: 其他函数都通过返回新 DataFrame 或显式 copy 实现，唯独它原地修改
- **修改**: 添加 `df = df.copy()` 保持一致性

### ✅ 4. add_bmi_group 改用 pd.cut 替代 apply
- **文件**: `task3.py`
- **问题**: 年龄分组用 `pd.cut`，BMI 分组用 `apply`，风格不统一且 `pd.cut` 性能更优
- **修改**: 统一改用 `pd.cut`

### ✅ 5. ever_married → "婚姻" 语义映射偏差
- **文件**: `task2.py`
- **问题**: `ever_married` 表示"是否曾结过婚"，`No→'未婚'` 容易误解为"当前未婚"
- **修改**: 将列名 `婚姻` 改为 `曾婚`，映射 `Yes→'曾婚'` / `No→'未曾婚'`

### ✅ 6. inspect_data 返回值被丢弃
- **文件**: `task1.py`
- **问题**: `inspect_data` 构建并返回 dict，但 `task1.run()` 没有使用
- **修改**: 让 `task1.run()` 返回 `(df, info_dict)` 元组，或简化去掉无用 return

---

## 三、分析深度增强

### ✅ 7. 年龄最小值 0.08 未被讨论
- **文件**: `task1.py`
- **问题**: 数据体检未提及存在婴儿记录
- **修改**: 在问题列讨论中增加对极端低龄的说明

### ✅ 8. 吸烟"未知"组（30.22%）缺乏分析
- **文件**: `task3.py`
- **问题**: 1544 条未知记录含 47 个中风患者，但完全没有被讨论
- **修改**: 在猜想3中增加对"未知"组的基本统计（样本数、中风率、平均年龄等）

### ✅ 9. "已戒烟者中风率更高"缺乏年龄分层交叉验证
- **文件**: `task3.py`
- **问题**: 代码指出年龄是混淆变量，但未做年龄分层分析来验证
- **修改**: 在猜想3后增加年龄分层 × 吸烟状态的交叉分析表

### ✅ 10. generate_report.py 分析逻辑与 task3 重复
- **文件**: `generate_report.py`
- **问题**: `load_data()` 完全重新实现了 task3 的分组和聚合逻辑
- **修改**: 改为直接复用 task3 中的分组函数和分析逻辑

---

## 不修改的项

- **高血压未纳入分析**: 作业要求的三个猜想（年龄/心脏病/吸烟）已覆盖，高血压属于"附加探索"范畴，非必需
- **性别分析缺失**: 同上，非强制要求
- **generate_report.py 字体路径**: 硬编码 macOS 路径在当前环境下正常运行，属于部署问题非代码错误
- **BMI 超重 > 肥胖异常**: 在附加探索中增加简要注释说明可能原因即可，不做深度分析
